# Karel Switcher environment overrides
# Copy this file to `.env` and edit values as needed.
# Lines starting with # are comments.

# Blackmagic ATEM switcher IP address
ATEM_IP=192.168.10.240

# Arduino serial device (Linux example below; on macOS use /dev/tty.usbmodemXXXX)
ARDUINO_PORT=/dev/ttyUSB0

# Optional: serial baud rate (defaults to 9600 if not set)
BAUD_RATE=9600

# Video file to be preloaded in mpv (used by run_mpv.py)
VIDEO_FILE=./test2.mp4

# Delay between CUT trigger and starting mpv playback (milliseconds)
# Set to 0 for immediate start.
MPV_PLAY_DELAY_MS=0

# Step size for Left/Right arrow seeks (milliseconds)
MPV_STEP_MS=100
